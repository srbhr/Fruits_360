# This project imports exactly ONE third-party package: tensorflow.
#
# Everything else it uses (json, pathlib, random, sys) is the Python standard
# library. numpy rides along as a TensorFlow dependency — we never import it
# directly. pillow and matplotlib are NOT used by this code at all, so they're
# deliberately absent (the old 2019 notebook plotted with them; the new
# predict.py just prints).

tensorflow>=2.16,<2.20   # the macOS arm64 wheel is just `tensorflow`; pulls numpy + its own deps

# --- Optional: Apple-Silicon GPU acceleration (Metal) ---
# The CPU already trains this tiny model in minutes, so this is optional.
# Metal lags TF releases and some op combos have bugs — pin a matching pair.
# tensorflow-metal>=1.1

# --- Optional: if you want the old grid-of-images prediction plot ---
# matplotlib>=3.8
